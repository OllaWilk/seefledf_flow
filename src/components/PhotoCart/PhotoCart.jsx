import React from 'react';
import { Button} from '../../components';

import './PhotoCart.scss';

const PhotoCart = ({img, imgDesc, abstract, content}) => {

  return (
    <div className="app__photoCart">
      
    </div>
  )
}

export default PhotoCart;

